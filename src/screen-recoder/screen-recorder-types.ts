export enum ScreenRecorderState {
    IDLE = "idle",
    STARTING = "starting",
    RECORDING = "recording",
    STOPPING = "stopping",
    STOPPED = "stopped",
    SAVING = "saving",
  }
  
  export enum ScreenRecorderType {
    DEFAULT = "default",
    FFMPEG = "ffmpeg",
  }
  